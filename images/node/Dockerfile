ARG BASE_TAG=latest
FROM ghcr.io/hansvd/claude-docker-base:${BASE_TAG}

# Node is already included in the base image.
# This image exists as an explicit stack target so users can
# specify `image: node` in their .claude-docker.yml config.
